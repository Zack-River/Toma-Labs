import { Link } from 'react-router-dom';

export function CartSummary({ totals, session, itemCount, checkoutOpen, onCheckout, onSignOut, message }) {
  return <aside className="cart-summary" aria-labelledby="summary-title"><p className="eyebrow">The order summary</p><h2 id="summary-title">Ready when you are.</h2><div className="summary-row"><span>Known subtotal</span><strong>{totals.hasPendingQuote ? '—' : totals.label}</strong></div><div className="summary-total"><span>Total preview</span><strong>{totals.hasPendingQuote ? '[QUOTE REQUIRED]' : totals.label}</strong></div><p className="quote-note">{totals.hasPendingQuote ? 'Some items still need a final quote.' : 'Temporary MVP showcase prices are attached so the cart and WhatsApp flow can be demonstrated.'}</p>{!checkoutOpen ? <button className="checkout-button" type="button" disabled={!itemCount} onClick={onCheckout}>Checkout via WhatsApp</button> : <p className="checkout-open-note">Complete your details below to prepare the WhatsApp order.</p>}{itemCount ? <p className="signed-in-note">{session ? <>Signed in as {session.email}. <button className="summary-account-link" type="button" onClick={onSignOut}>Sign out</button></> : <>Guest checkout is available. <Link to="/login?return=%2Fcart">Sign in</Link> to save your bag.</>}</p> : null}<p className="cart-toast" role="status" aria-live="polite">{message}</p></aside>;
}
