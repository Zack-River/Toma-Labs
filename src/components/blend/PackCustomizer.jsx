import { TomaIcon } from '../TomaIcon.jsx';
import { faArrowUpRightFromSquare } from '@fortawesome/free-solid-svg-icons';

export function PackCustomizer({ draft, onUpdate, onReview, onBack, config, goalMessage }) {
  return <><div className="step-heading"><p className="eyebrow">Step 04 / Pack</p><h2>Make this blend yours.</h2><p>Give the recipe a size and a name. Pricing is shown as a guide estimate while live pricing is connected.</p></div><div className="pack-choice"><div className="choice-heading"><span className="field-label">Pack size</span><span>Configured availability</span></div><div className="pack-options" role="group" aria-label="Pack size">{config.packSizes.map((pack) => <button type="button" key={pack.grams} onClick={() => onUpdate({ packSizeGrams: pack.grams })} aria-pressed={draft.packSizeGrams === pack.grams} disabled={!pack.available}>{pack.label}</button>)}</div></div><div className="name-field"><label htmlFor="blend-name">Blend name <span>optional</span></label><input id="blend-name" name="blend-name" type="text" maxLength="24" minLength="2" placeholder="Your Name’s Blend" autoComplete="off" value={draft.blendName || ''} onChange={(event) => onUpdate({ blendName: event.target.value })} /><div className="field-help"><span>2–24 characters</span><output>{(draft.blendName || '').length} / 24</output></div></div><div className="label-line"><label htmlFor="label-line">Label line <span>from your selected moment</span></label><input id="label-line" type="text" value={goalMessage} readOnly /></div><div className="panel-actions"><button className="button button-brass" type="button" onClick={onReview}>Review my blend <TomaIcon icon={faArrowUpRightFromSquare} /></button><button className="text-link muted" type="button" onClick={() => onBack('character')}>Back to character</button></div></>;
}
