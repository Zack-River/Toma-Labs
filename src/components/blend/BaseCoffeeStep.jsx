import { TomaIcon } from '../TomaIcon.jsx';
import { faArrowTurnDown, faArrowUpRightFromSquare, faCircleDot } from '@fortawesome/free-solid-svg-icons';

export function BaseCoffeeStep({ onNext, onBack }) {
  return <><div className="step-heading"><p className="eyebrow">Step 02 / Coffee</p><h2>How do you want this coffee to show up in your cup?</h2><p>Start with the format TOMA is prepared to guide today.</p></div><div className="format-card selected" aria-label="Selected coffee format"><div className="format-icon" aria-hidden="true"><span></span></div><div><p className="micro-label">AVAILABLE FORMAT</p><h3>Turkish coffee</h3><p>Fine-ground coffee for a small, concentrated cup with a clear crema surface.</p></div><span className="selected-label">Selected</span></div><div className="grind-row"><div><p className="micro-label">GRIND</p><strong>Fine Turkish grind</strong><p>Set by the selected format.</p></div><TomaIcon icon={faArrowTurnDown} className="lock-mark" label="Locked by format" /></div><div className="config-note"><TomaIcon icon={faCircleDot} /><p>More formats will appear here only when they are available in TOMA’s live catalog.</p></div><div className="panel-actions"><button className="button button-brass" type="button" onClick={() => onNext('character')}>Continue to character <TomaIcon icon={faArrowUpRightFromSquare} /></button><button className="text-link muted" type="button" onClick={() => onBack('moment')}>Back to moment</button></div></>;
}
